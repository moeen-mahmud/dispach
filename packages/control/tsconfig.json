{
    "extends": "../../tsconfig.base.json",
    "compilerOptions": {
        // Shipped as source: Node 24 strips the types, so nothing builds and nothing is emitted.
        "noEmit": true,
        "declaration": false,
        "declarationMap": false,
        "types": ["bun"]
    },
    "include": ["src", "test"]
}
