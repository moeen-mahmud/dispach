/**
 * The workspace templates, embedded.
 *
 * Embedded rather than read from disk because the installed binary ships only `dist/` — an
 * `init` run in an arbitrary directory has no `examples/` to read. The human-edited source of
 * truth stays `examples/workspace-template/`; `test/templates.test.ts` asserts byte-equality
 * between that directory and these constants, so an edit to one without the other fails CI
 * rather than shipping a scaffold that silently drifted from the documented templates.
 *
 * These are the *un-substituted* forms, `{{PLACEHOLDER}}` tokens intact. Substitution is
 * `fillTemplate`'s job, and any token it does not fill survives into the generated file on
 * purpose — the `workspace` command warns about unfilled placeholders until a person writes
 * the real text, which is the nag-until-personalised mechanism working as designed.
 */

/**
 * The starter skill `init --skills starter` scaffolds.
 *
 * Embedded for the same reason as the workspace templates, and byte-equal to
 * `examples/workspace-template/skills/starter/SKILL.md` by the same test. It carries no
 * `{{PLACEHOLDER}}` tokens: a skill is a procedure nobody can guess from the agent's name, so there is
 * nothing to substitute and the whole file is deliberately written as instructions to a person.
 */
export const SKILL_TEMPLATE =
    '---\nname: {{SKILL_NAME}}\ndescription: Replace this description with what the skill does and when to use it, naming the specific words someone would type \u2014 file formats, tool names, the error they are looking at. Selection is lexical and this sentence is all it has.\nmetadata:\n  "{{WHEN_NOT_TO_USE_KEY}}": >\n    Replace this too. Say what this skill is NOT for, and name the skill or the plain answer\n    that handles those cases instead.\n---\n\n<!--\nSKILL.md \u2014 one procedure the agent follows on the turns that call for it.\n\nThe format is agentskills.io, so this folder is portable to other clients. The spec\'s fields are\n`name` (which must equal this directory\'s name), `description`, and optionally `license`,\n`compatibility`, `metadata` and `allowed-tools`. Anything else is ignored rather than refused.\n\nHOW SELECTION WORKS, because it changes how you write the description. The harness ranks every\nskill\'s name and description against what was just asked, picks at most `skills.maxActive`, and\ninjects the winner. The model does not choose \u2014 so the description is a routing signal aimed at a\nlexical scorer, not a pitch aimed at a reader. Concrete nouns beat adjectives. A description whose\nonly distinctive word is a common verb activates on any turn using that verb: measured, a skill\nopening "Write and debug a Dockerfile" was selected for "write me a haiku about autumn".\n\nNegative guidance is not a spec field, which is why it lives under `metadata`. It is worth writing\nanyway \u2014 negative examples are the cheapest routing improvement available \u2014 and `skills validate`\nwarns when it is missing. It is never required to load, so a skill vendored from elsewhere works\nas it arrives.\n\nTHE BODY IS INJECTED VERBATIM on every turn this skill activates, so every line below is paid for.\nWrite steps, not background. Detail that is only occasionally needed goes in `references/` for the\nmodel to read on demand. These HTML comments are stripped before the model sees the file, so\nauthoring notes like this one are free.\n\nSCRIPTS. Drop an executable into `scripts/` and it becomes a tool named\n`skill.{{SKILL_NAME}}.<filename>`, callable only while this skill is active. A `.py` runs under `python3`,\nor under `uv run` if this folder has a `pyproject.toml` or `requirements.txt`; `.ts` and `.js` run\nunder the host; anything else needs an executable bit and a shebang. A file in `scripts/` that\nmatches none of those will never run, and `skills validate` says so. A deterministic script beats\ninstructions the model has to interpret \u2014 prefer one wherever the work is mechanical.\n-->\n\nReplace everything below with the steps.\n\n1. State the first step as an action, not a topic.\n2. Where a step has a rule that was learned the hard way, say the rule and why \u2014 a step with a\n   reason generalises to the case you did not think of.\n3. Say what to report at the end, and what not to do. A procedure that does not say where it stops\n   gets carried into the next turn.\n'

export const WORKSPACE_TEMPLATE_FILES = [
    "AGENTS.md",
    "POLICY.md",
    "USER.md",
    "MEMORY.md",
    "REMINDER.md",
    "SOUL.md",
    "SOUL.compact.md",
] as const

export type WorkspaceTemplateName = (typeof WORKSPACE_TEMPLATE_FILES)[number]

export const WORKSPACE_TEMPLATES: Record<WorkspaceTemplateName, string> = {
    "AGENTS.md":
        '---\ntier: static\neditable: none\nbudget: 800\n---\n\n<!--\nAGENTS.md \u2014 what this agent DOES: responsibilities and operating procedure.\n\nTIER 0. Sent every turn, cached, read-only to the agent. Budget: 800 tokens.\n\nThe identity lives in SOUL.md; this file is deliberately free of personality. The split\nis the one the wider ecosystem converged on: if SOUL.md answers "who are you?", AGENTS.md\nanswers "what do you do, and how?" Mixing them makes both files harder to maintain \u2014 a\ntone change should never touch a procedure, and a workflow change should never risk the\nvoice.\n\nWHAT THIS FILE IS FOR: the agent\'s job, stated as scope; how it works through a task;\nwhen and where it records things. Operational truth that holds whatever mood the prose\nin SOUL.md is in.\n\nWHAT IT IS NOT FOR: rules and boundaries (POLICY.md and the soul\'s <rules> block own\nthose, and the rule counter budgets them), facts about the user (USER.md), or anything\nenforced in code \u2014 a procedure written here is advisory the way all prompt text is.\n\nSTYLE: declarative first person \u2014 "I check X before Y", never "Check X before Y". This\nis not cosmetic: obligation-shaped lines (imperative openers, must/never/always) count\nagainst the same rule budget as everything else in the static tier, and a procedures\nfile written as commands reads as twenty rules. Written as description, it reads as one\njob. The workspace command\'s authoring checks will tell you if a line counts.\n-->\n\n# What I do\n\n{{RESPONSIBILITIES}}\n\n# How I work\n\n{{WORKFLOW}}\n\n# How I use memory\n\n{{MEMORY_PROCEDURE}}\n\n<!-- \u2500\u2500 Team \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nSub-agent responsibilities and handoff routing live here once delegation ships. This\nsection stays commented out until then: comments are stripped before the model sees the\nfile, and a visible instruction to "hand off to the research agent" on a runtime that\ncannot delegate yet produces confidently narrated handoffs that never happened.\n\n# Team\n\n- <sub-agent>: <what it owns, and what I hand it>\n- <sub-agent>: <what it owns, and what I hand it>\n\nWhat comes back from a sub-agent I treat as a draft to check, not a fact to repeat.\n\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500 -->\n',
    "POLICY.md":
        "---\ntier: static\neditable: none\nbudget: 600\n---\n\n<!--\nPOLICY.md \u2014 the few standing boundaries worth prompt tokens.\n\nTIER 0. Sent every turn, cached, read-only. Budget: 600 tokens.\n\nREAD THIS BEFORE WRITING ANYTHING HERE.\n\nThis file is NOT a security control. Prompt-level restrictions are advisory: automated\nattacks defeat them on open-weight models at very high success rates, and patient\nmulti-turn pressure defeats them with no tooling at all. Anything you need to be TRUE\nmust be enforced in code at the tool boundary.\n\n  Belongs in code, not here          How to enforce it\n  ---------------------------------  --------------------------------------------\n  Cannot spend money                 no payment tool, or approval middleware\n  Cannot email arbitrary people      recipient allowlist in the tool\n  Cannot delete data                 no destructive tool bound to this agent\n  Cannot read other users' data      scoped credentials, per agent\n  Cannot exceed a budget             usage tracking in middleware\n  Cannot touch production            separate credentials per environment\n\n  Belongs here\n  ---------------------------------\n  Tone under pressure\n  When to defer rather than act\n  What to decline, and how\n  What to do when uncertain\n\nIf you find yourself writing \"never\" about something that would be expensive if it\nhappened, stop and write a wrapToolCall middleware instead.\n\nSTATE THE REASON alongside each boundary. A boundary with a rationale generalises to\nsituations you didn't enumerate; a bare prohibition only covers what it names.\n\nSTYLE: match the target output, same as the identity file. Prose for chat agents.\n\nBUDGET: two or three items. They count against the same rule budget as the identity\nfile, AGENTS.md, and\nREMINDER.md. Many agents need none \u2014 delete the file and the loader skips it.\n-->\n\n# Policy\n\n<!--\nSoft boundaries, each with its reason.\n\n  Weak:    Never share information about other people.\n  Better:  If something involves a person other than the user, ask what they want shared\n           before including it \u2014 the user knows the relationship and I don't.\n-->\n\n{{BOUNDARIES}}\n\n<!--\nOne line: what the agent does when it doesn't know, can't reach a tool, or hits an\nambiguous request. A defined fallback measurably reduces confabulation and gives the\nagent somewhere to go other than guessing.\n-->\n\n{{UNCERTAINTY_BEHAVIOUR}}\n",
    "USER.md":
        '---\ntier: volatile\neditable: append\nbudget: 1500\n---\n\n<!--\nUSER.md — what the agent knows about the person it works for.\n\nTIER 1. Sent every turn, UNCACHED. Budget: 1500 tokens.\n\nYou write this file. The agent reads it every turn. memory_write does not land\nhere — that tool writes MEMORY.md, which declared eviction: oldest. This file\nis appendable so you can add a standing fact by hand; notes the agent saves\nhave a budget and an overflow, and this file has neither.\n\nThis file sits AFTER cache breakpoint A because the volatile tier changes:\nMEMORY.md is rewritten on every save, and a changing file ahead of the\nbreakpoint would invalidate the cached prefix on every write with no error\nand no symptom beyond the bill.\n\nWHAT THIS FILE IS FOR: standing facts about the user — name, role, timezone,\ncontext, preferences they should not have to restate. Facts, not transcript:\n"prefers answers under a paragraph" belongs here; "asked about the weather on\nTuesday" does not.\n\nWHAT IT IS NOT FOR: rules. An obligation written here escapes the rule budget\'s\ncount (volatile is excluded — it holds facts about the person, not policy) but\nthe model reads it like any other instruction, so a rule hidden here is a rule\nnobody budgeted for. Put rules in the soul\'s <rules> block and let the guard\ncount them.\n\nWHAT IT IS NOT FOR: working notes. A save that lands here has no eviction and\ngrows until the next load fails. Promote a settled fact from MEMORY.md into\nthis file by hand.\n\nFORMAT: short declarative lines, one fact per line, in prose — not bullets.\nModels imitate form. Keep the authored part small and stable — every token\nhere is paid on every turn.\n-->\n\n# {{USER}}\n\n{{USER_FACTS}}\n\n<!--\nAdd standing facts below, one prose sentence each. Leave a line out rather than\nguessing. Timezone, how they like to be addressed, role and recurring people,\nlength and tone, tools they always want or never want.\n-->\n',
    "MEMORY.md":
        "---\ntier: volatile\neditable: replace\nbudget: 2000\neviction: oldest\n---\n\n<!--\nMEMORY.md — the agent's working memory.\n\nTIER 1. Sent every turn, UNCACHED, agent-writable (replace + oldest-first eviction).\nBudget: 2000 tokens.\n\nmemory_write lands here. The runtime resolves ONE write target — the writable\nvolatile file that declared `eviction: oldest` — and the model never chooses a\nfilename, because choosing would be a second decision on every save and that is\nthe two-hop shape small models fail. Declared order does not decide it: USER.md\nis listed first and is writable, and saves still land HERE. That is why USER.md\ndoes not grow until the agent refuses to boot.\n\nWHAT THIS FILE IS FOR: state that matters across sessions but is not a durable\nfact about the user — open threads, decisions taken, things promised. It is\ncapped and evicting by design: working memory that only ever grows becomes a\nsecond conversation history, paid for on every turn.\n\nWHAT IT IS NOT FOR: an archive. When something stops being working state and\nbecomes a fact about the person, it belongs in USER.md; when it is reference\nmaterial needed only sometimes, it belongs in knowledge/, which costs nothing\non the turns that don't mention it.\n\nLeave the body empty below this comment. The agent fills it; you don't.\n-->\n",
    "REMINDER.md":
        "---\ntier: reminder\neditable: none\nbudget: 500\n---\n\n<!--\nREMINDER.md \u2014 one or two rules, re-asserted where attention is strongest.\n\nTIER 2. Injected AFTER the conversation history, before retrieved memory and the current input.\nBudget: 500 tokens \u2014 and using a fraction of it is the point.\n\nRule adherence decays over a conversation and compaction does not reliably reset it.\nAttention is stronger at both ends of the context than in the middle, so a rule stated\nonce at the top of a thirty-turn session is effectively in the middle. This file re-states\nthe one or two rules that must survive turn thirty, at the recency position.\n\nONE OR TWO RULES. This is not a second policy file: everything here counts against the\nsame rule budget as the rest of the static tier (the model does not know the text came from\ndifferent files), and a reminder tier that grows into a policy file spends the position\nit exists to exploit.\n\nPick the rule whose failure is most expensive \u2014 usually the confirm-before-consequences\none \u2014 and restate it in the same words the identity file uses. Same words, not a paraphrase: two\nphrasings of one rule read as two rules.\n\nOmit this file entirely for short-lived agents; drift isn't a problem under ~10 turns.\n-->\n\n{{REMINDER_RULE}}\n",
    "SOUL.md":
        '---\ntier: static\nbudget: 1400\n---\n\n<!--\nSOUL.md \u2014 the long-form identity document, in the style of a model constitution.\n\nThis file ships ONLY to a model that meets context.soul.requires (a big window on a\nfrontier-class id). Anything else gets SOUL.compact.md instead and never sees this file.\nIts premise is that a capable model, given enough understanding of the goals, can derive\nrules the author never wrote \u2014 so write it as explanation, not instruction.\n\nThe <rules> block is the exception: rules survive distillation verbatim and hold on every\nmodel. Keep each rule to ONE LINE \u2014 the rule counter is line-based. The prose outside\n<rules> is exempt from rule counting for this file only; the compact file enjoys no such\nexemption.\n\nA soul owns WHO the agent is. Operations \u2014 responsibilities, workflows, team routing \u2014\nlive in AGENTS.md, which coexists with the soul; what must not exist is a second identity\ndocument in the same prefix, because two identities contradict each other.\n\nEvery paragraph here is a starting point, not a script. The fastest way to a distinctive\nagent is rewriting "How I sound" in the voice you actually want back, then re-deriving\nSOUL.compact.md by hand to match. Edit this file first, the compact one second \u2014 never\nthe reverse.\n-->\n\n# Who {{AGENT_NAME}} is\n\n{{SOUL_WHO}}\n\n{{SOUL_MEASURE}}\n\n# How I think about answers\n\n{{SOUL_ANSWERS}}\n\n<rules>\n{{RULE_CONFIRM}}\n{{RULE_HONESTY}}\n{{RULE_MEMORY}}\n</rules>\n\n# How I sound\n\n{{SOUL_VOICE}}\n\n## How I talk\n\n<!--\nThree short exchanges, in the voice above. This is the highest-leverage section in the\nfile and the main defence against generic output \u2014 an example demonstrates a voice that\nadjectives can only describe. Keep them varied: three examples about one topic produce\nan agent that steers every conversation toward it. The placeholders below are deliberate:\nthe workspace command warns until you replace them with real exchanges.\n-->\n\n<example>\n{{USER}}: {{INPUT_1}}\n{{AGENT_NAME}}: {{REPLY_1}}\n</example>\n\n<example>\n{{USER}}: {{INPUT_2}}\n{{AGENT_NAME}}: {{REPLY_2}}\n</example>\n\n<example>\n{{USER}}: {{INPUT_3}}\n{{AGENT_NAME}}: {{REPLY_3}}\n</example>\n\n# What I refuse to become\n\n{{SOUL_REFUSE}}\n',
    "SOUL.compact.md":
        "---\ntier: static\nbudget: 800\n---\n\n<!--\nCompact identity, derived from SOUL.md. This is the file small models actually run on, so\nevery sentence here is paid for on every turn \u2014 keep it to the few that produce the voice.\nThe long document stays the source of truth: edit it first, then re-derive this by hand.\n\nUnlike SOUL.md, EVERY line here counts against the rule budget, not just the <rules>\nblock \u2014 a small model does not get the benefit of the derivation exemption, because it is\nprecisely the model that cannot derive.\n-->\n\n# Who {{AGENT_NAME}} is\n\n{{SOUL_COMPACT_WHO}}\n\n# How I think about answers\n\n{{SOUL_COMPACT_ANSWERS}}\n\n<rules>\n{{RULE_CONFIRM}}\n{{RULE_HONESTY}}\n{{RULE_MEMORY}}\n</rules>\n\n# How I sound\n\n{{SOUL_COMPACT_VOICE}}\n\n## How I talk\n\n<!-- The same three exchanges as SOUL.md, or trimmed variants \u2014 same voice, fewer words. -->\n\n<example>\n{{USER}}: {{INPUT_1}}\n{{AGENT_NAME}}: {{REPLY_1}}\n</example>\n\n<example>\n{{USER}}: {{INPUT_2}}\n{{AGENT_NAME}}: {{REPLY_2}}\n</example>\n\n<example>\n{{USER}}: {{INPUT_3}}\n{{AGENT_NAME}}: {{REPLY_3}}\n</example>\n\n# What I refuse to become\n\n{{SOUL_REFUSE}}\n",
}

/**
 * Replace `{{NAME}}` tokens with values; tokens without a value survive untouched.
 *
 * Deliberately not an error path: a surviving token is the scaffold contract, not a failure.
 * The regex is the same shape the authoring check uses to *find* placeholders, so what this
 * function leaves behind is exactly what `checkAuthoring` will report.
 */
export function fillTemplate(text: string, values: Record<string, string>): string {
    return text.replace(/\{\{\s*([A-Za-z0-9_]+)\s*\}\}/g, (token, name: string) => {
        const value = values[name]
        return value === undefined ? token : value
    })
}
