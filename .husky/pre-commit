# Fast only. A pre-commit hook that takes 40 seconds is one people learn to skip with
# `--no-verify`, and a skipped hook checks nothing at all. The gate lives in pre-push and in CI.
bunx lint-staged
